# توافق الرومات — نواة POCO F5 (marble) GKI 6.18

## المقصود بـ «نواة عامة»

يستهدف هذا المشروع **جهاز POCO F5 / Redmi Note 12 Turbo (`marble`) فقط**، ويهدف إلى أن يكون نواة Android/GKI قابلة للتكامل مع أي روم Android متوافق لهذا الجهاز، بما في ذلك الرومات المستندة إلى AOSP أو التوزيعات المشتقة منه. لا ترتبط شجرة المصدر بروم محدد ولا تحتوي أي إعداد أو تغليف خاص بروم بعينه.

> النواة العامة لا تعني ملف `boot.img` واحداً يمكن تفليشه على جميع الرومات بلا تكييف. توافق Android يعتمد على نسخة Android وواجهة KMI وصور `vendor_boot` و`dtbo` ووحدات `vendor_dlkm` الخاصة بالروم المثبتة.

## عقد التوافق

| طبقة التوافق | ما يقدمه هذا المشروع | ما يوفره مستخدم الروم أو مُشرفه |
|---|---|---|
| النواة الأساسية | قاعدة Android Common Kernel 6.18 وإعداد GKI عام لـ arm64 وواجهة نقل marble. | تحديد إصدار Android/ACK الموافق للروم المستهدف. |
| تعريفات marble | هدف موحد لنقل Device Tree وSM7475 والتعريفات المشتركة. | رقع التعريفات والوحدات المتوافقة مع الكاميرا والعرض والشبكة والصوت والطاقة. |
| KMI والوحدات | مسار لإعادة بناء الوحدات مع النواة عند اكتمال النقل. | وحدات `vendor_dlkm` الموافقة لـ KMI والبنية المختارة. |
| التغليف والتفليش | لا يُضمَّن `boot.img` أو `vendor_boot.img` عام في المشروع. | صور الأقسام، إصدار boot header، AVB، ووسيلة استرداد المطابقة للروم المحدد. |

## حالة المشروع الحالية

النموذج الحالي ينجح في بناء صورة GKI arm64 عامة، ويحتوي على Binder وUFS وGENI serial وremoteproc من إعداد GKI. لكنه لا يضم بعد Device Tree خاصاً بـ SM7475/marble أو تعريفات Xiaomi/Qualcomm الضرورية للرسوميات والشبكات والعرض والكاميرا والصوت والمودم.

لذلك، لا توجد حالياً صورة تفليش عامة أو صورة خاصة بروم واحد. هذه نتيجة مقصودة: منع إنتاج ملف يوحي بتوافق غير متحقق قبل اكتمال نقل اللوحة واختبارات الإقلاع.

## متطلبات دمج أي روم

عند البدء بدعم روم محدد، تُجمع بياناته المطابقة تماماً من نفس البناء: بصمة النظام، مستوى Android، وإصدار KMI، و`boot.img` أو `init_boot.img` عند وجوده، و`vendor_boot.img`، و`dtbo.img`، و`vendor_dlkm.img`. تستخدم هذه البيانات لتجهيز تغليف واختبار خاص بالروم **من دون تعديل أساس النواة العامة**.

لا يجوز تفليش مخرجات البناء العامة من هذه الشجرة مباشرة. يبدأ الاختبار فقط بعد اكتمال تعريفات marble وإعادة بناء الوحدات وتحقق KMI وإنشاء مسار استرداد خاص بالروم المراد اختباره.
