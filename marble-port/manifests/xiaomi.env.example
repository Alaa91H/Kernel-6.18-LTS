# Copy outside the repository and fill only after static, KMI, and device acceptance.
ROM_FLAVOR=xiaomi
DEVICE=marble
# Absolute paths to artifacts extracted from the exact target ROM build.
BOOT_IMAGE=
VENDOR_BOOT_IMAGE=
DTBO_IMAGE=
VENDOR_DLKM_DIR=
SYSTEM_DLKM_DIR=
# Document the AVB/signing policy; do not store keys in Git.
AVB_POLICY=
SIGNING_KEY_PATH=
# Record hashes of all input images outside this file.
INPUT_ARTIFACT_MANIFEST=
